seeds:
  - name: city_region
    description: Города и регионы
    config:
      column_types:
        city: varchar(50)
        region: varchar(50)
      quote_columns: true
      delimeter: ','
    columns:
      - name: city
        tests:
          - not_null
          - unique
